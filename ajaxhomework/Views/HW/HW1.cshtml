@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@


<div class="container-fluid">
<div class="card-group" id="container">

</div>
</div>



@section Scripts{
    <script src="~/js/travel.js"></script>
    <script>
        //console.log(spots.CommonFormat.Section[0].file.img[0].text)
       // 找到要控制的對象
        const datas= spots.CommonFormat.Section.map(m =>{
            const {stitle,xbody,file} = m;
            const img = file.img
            //console.log(file.img.toString())
            return (
                `                       <div class="card">
                                                        <img src= "${file.img[0]?.text}" class = "card-img-top" alt = "..." id = "img" >
                                        <div class="card-body" >
                                        <h5 class="card-title" id = "title" >${stitle}</h5>
                                        <p class="card-text" id = "body" > ${xbody}</p>
                                        
                                        </div>
                        `
            )
        })
        document.querySelector('#container').innerHTML = datas.join("");

       
    </script>
}
